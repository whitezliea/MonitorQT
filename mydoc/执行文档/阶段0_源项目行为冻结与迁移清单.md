# 阶段 0：源项目行为冻结与迁移清单

执行日期：2026-06-25  
源项目：`MyProject/`，WPF / C# / .NET 10  
目标项目：当前 `MonitorQT`，Qt6 / C++ / CMake  
代码合同：`phase0/SourceBehaviorFreeze.*`

## 1. 阶段 0 完成范围

- 已冻结 WPF 源项目的核心运行节奏：500 ms 采集、3 个周期超时、1 s UI 刷新、5 s 批量写入、30 天历史保留、1/5/30 分钟趋势窗口。
- 已冻结默认业务字典：22 个 `TagDefinition` 与 22 个 `TagSourceMapping`。
- 已冻结七个页面的 parity checklist。
- 已按 C# 测试文件分类冻结 Qt 版必须复刻的测试清单，源项目共 157 个 `[Fact]` / `[Theory]`。
- 已提供一帧最小验收 `RawMeasurementFrame`，可在后续阶段验证 Tag、派生量、矩阵统计、报警状态和历史采样候选。
- 已在应用启动时调用 `Phase0::validateSourceBehaviorFreeze()`，合同不一致时直接返回错误。

## 2. 默认参数冻结

| 参数 | 冻结值 | 验收点 |
| --- | ---: | --- |
| 数据生成周期 | 500 ms | 模拟器默认采样周期 |
| 数据源超时倍数 | 3 | 1.5 s 未收到帧标记 Offline |
| UI 刷新周期 | 1000 ms | Qt `QTimer` 壳层刷新 |
| 历史批量写入周期 | 5000 ms | 后台 Worker 周期 |
| 历史批量上限 | 100 条 | 达量提前 flush |
| 历史保留天数 | 30 天 | 保留任务清理 |
| 警报批量写入周期 | 5000 ms | 后台 Worker 周期 |
| 警报批量上限 | 100 条 | 达量提前 flush |
| 操作日志批量写入周期 | 5000 ms | 后台 Worker 周期 |
| 操作日志批量上限 | 100 条 | 达量提前 flush |
| 趋势窗口 | 1 / 5 / 30 分钟 | 500 ms 下分别为 120 / 600 / 3600 点 |

## 3. 模块映射冻结

| C# 模块 | Qt 目标模块 | 必须保持 |
| --- | --- | --- |
| `Domain/Measurements` | `domain/measurements` | `RawMeasurementFrame`、6 通道、16 x 16 矩阵、UTC 时间 |
| `Domain/Tags` | `domain/tags` | TagId、来源映射、质量、报警状态和值类型 |
| `Domain/Alarms` | `domain/alarms` | Active / Acknowledged / Recovered 生命周期 |
| `Domain/Rules` | `domain/rules` | 质量规则、阈值规则、矩阵统计 |
| `Application/Configuration` | `application/configuration` | 默认运行参数和动态配置 |
| `Application/Services/TagDefinitionCatalog.cs` | `application/services/TagDefinitionCatalog.*` | 22 个 Tag 与 22 个 Mapping |
| `Application/Pipelines/DataCleanPipeline.cs` | `application/pipelines/DataCleanPipeline.*` | 原始帧映射、派生量、质量传播、矩阵统计 |
| `Application/Services/AlarmService.cs` | `application/services/AlarmService.*` | 质量警报、阈值警报、确认和恢复 |
| `Application/Services/UiSnapshotProvider.cs` | `application/services/UiSnapshotProvider.*` | UI 只读统一快照 |
| `Application/Queues` / `BackgroundWorkers` | `application/queues` / `application/workers` | 5 s 或 100 条 flush，停止时最终 flush |
| `Infrastructure/Persistence` | `infrastructure/persistence` | SQLite schema v5、WAL、分页查询、设置持久化 |
| `Simulator/Generators` / `Scenarios` | `simulator/generators` / `simulator/scenarios` | 默认设备 `MCMD-001`、异常场景和 UTC |
| `MultiChannelMonitor/ViewModels` | `presentation/viewmodels` 或 presenters | 页面状态、命令和快照刷新 |
| `MultiChannelMonitor/Views` | `presentation/pages` | 七个页面能力，不逐行翻译 XAML |
| `MultiChannelMonitor/Renderers` | `presentation/renderers` | 趋势和热力图只绘制快照 |

## 4. 页面功能清单

| 页面 | Qt 页面 | 必须保留能力 |
| --- | --- | --- |
| Dashboard | `DashboardPageWidget` | 指标卡、关键 Tag、活跃警报、趋势预览、矩阵预览 |
| Realtime Tags | `RealtimeTagsPageWidget` | Tag 表格、分类筛选、文本筛选、质量/报警状态、跳转趋势 |
| Trend | `TrendPageWidget` | Tag 和窗口选择、实时曲线、阈值线、质量点、尖峰诊断、跳转历史 |
| Alarm Center | `AlarmCenterPageWidget` | 当前警报、历史查询、分页、取消查询、确认警报 |
| History | `HistoryPageWidget` | Tag/时间查询、分页、取消查询、趋势预览、CSV 导出 |
| Measurement Map | `MeasurementMapPageWidget` | 16 x 16 热力图、量程、统计摘要、异常点、单元格选择 |
| Logs & Settings | `LogsSettingsPageWidget` | 业务日志查询、Tag 阈值、历史策略、运行参数保存 |

## 5. 测试复刻分类

| 分类 | 用例数 | Qt 复刻优先级 |
| --- | ---: | --- |
| Domain | 6 | 必须：矩阵模型、统计和领域约束 |
| Application | 107 | 必须：清洗、报警、快照、运行时、持久化 Worker、UTC；应该：趋势诊断与图表服务 |
| Infrastructure | 23 | 必须：SQLite schema v5、事务、分页、配置和仓储 |
| Simulator | 9 | 必须：6 通道、16 x 16 矩阵、异常场景和 UTC |
| Specification | 10 | 必须：Tag 字典、RawCode 映射、运行状态和持久化边界 |
| AppLogging | 2 | 可后置：日志门面替代 |
| 合计 | 157 | 与源项目 `[Fact]` / `[Theory]` 数量一致 |

## 6. 最小验收数据链路

`Phase0::createMinimumAcceptanceFrame()` 固定生成一帧：

| 字段 | 值 |
| --- | --- |
| DeviceId | `MCMD-001` |
| SequenceNo | `1` |
| Timestamp | `2026-06-25T00:00:00Z` |
| DeviceStatus / Quality / ErrorCode | `Running` / `Good` / `0` |
| 通道 | `TEMP_CH01=25.4 ℃`，`PRESSURE_CH01=101.2 kPa`，`LIGHT_CH01=580.6 lux`，`VOLTAGE_CH01=12.1 V`，`CURRENT_CH01=1.2 A`，`VIBRATION_CH01=0.03 mm/s` |
| 矩阵 | 16 x 16，`value(row,col)=600 + row*2 + col` |

该帧经过 `Phase0::evaluateMinimumAcceptanceFrame()` 后必须得到：

| 验收项 | 固定结果 |
| --- | ---: |
| CleanedTagValue 数量 | 22 |
| `MEAS.POWER.CH01` | 14.52 W |
| `MEAS.LOAD_RATIO.CH01` | 24.0 % |
| `MATRIX.LIGHT.AVG` | 622.5 lux |
| `MATRIX.LIGHT.MIN` | 600 lux |
| `MATRIX.LIGHT.MAX` | 645 lux |
| `MATRIX.LIGHT.UNIFORMITY` | 600 / 645 |
| `MATRIX.LIGHT.ABNORMAL_COUNT` | 0 |
| `MATRIX.LIGHT.HOTSPOT_ROW/COL` | 15 / 15 |
| 关键质量和警报状态 | `Good` / `Normal` |
| 历史采样候选 | 包含 `MEAS.TEMP.CH01`、`MATRIX.LIGHT.AVG`，不包含未历史化的 `DEVICE.STATUS` |

## 7. 当前 Qt 壳确认

- 当前路线采用 Qt Widgets。
- 当前 `QMainWindow + QStackedWidget + shell/nav/pages` 保留为迁移基础壳。
- `MainWindow` 的 UI 刷新周期已从 `Phase0::defaultRuntimeOptions().uiRefreshIntervalMs` 读取。
- 七个页面当前仍可用 `PagePlaceholderWidget` 导航，后续阶段逐页替换。
